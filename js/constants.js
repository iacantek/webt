export const RequiredValidationMessage = Object.freeze(`Das Feld "%s" darf nicht leer sein!`);

export const HeightUnit = Object.freeze({
    Cm: 0,
    Ft: 1
});

export const WeightUnit = Object.freeze({
    Kg: 0,
    Lbs: 1
});